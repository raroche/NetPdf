# NetPdf — Progress Status

> **Phase 3 CLOSED; Phase 4 (Visual Parity) is now the active phase (2026-06-25).** Phase 3's 11 exit criteria are all MET + `0.7.0-beta` is tagged (`af1c210`); the residual long-tail's last multicol PR **[#224](https://github.com/raroche/NetPdf/pull/224) MERGED (`caf99f1`)** — column rules (`column-rule-*`) + font-relative `column-width` / `column-gap` (+ a 2×P1/P2/P3 review: rules span the column box not the content; explicit `font-size: 0` preserved; rules composed with the multicol transform; docs narrowed). `multicol-balancing-pagination` stays open for `column-span: all`; **ExpectedDeferralIds 23**. The remaining Phase-3 deferrals are deep / L19-blocked / gated-on-Roland / post-v1 — none block anything (see `deferrals.md` + memory `project-phase3-residual-longtail`). **Phase 4 status:** 2D transforms (#210), linear + radial gradients (#209), sharp + blurred box-/text-shadow (#210), and backgrounds (Phase-3 interleaved) are MERGED. The bulk is greenfield — `filter` / `clip-path` / `mask` / `opacity` / `mix-blend-mode` / `border-image` aren't registered, `NetPdf.Svg` is a placeholder, and there are no Link annotations / outlines / conic gradients / visual-regression harness. **Phase-4 roadmap (8 ordered 3-task PR groups) — see the new section below + [docs/phases/phase-4-visual-parity.md](docs/phases/phase-4-visual-parity.md).** **PR 1 (gradient/shadow refinements — conic raster, length stops, repeating linear/radial, radial ellipse-via-CTM, box-shadow inset) MERGED ([#226](https://github.com/raroche/NetPdf/pull/226), `504fbd0`; +a 5-finding review round: repeating period = last−first, out-of-range stops kept raw + clipped, radial length stops vs rx, conic over-cap severity, stale msg).** **PR 2 (CSS filters on IMAGES) MERGED ([#227](https://github.com/raroche/NetPdf/pull/227), `49750e0`; +a 6-finding review round: SKCodec preflight before decode, drop-shadow σ=blur not 2σ, cache key from resolved steps, defaults=1 + clamp, unparseable-filter diagnostic, intrinsic-px downgrade).** **PR 3 (borders + clip-path) MERGED ([#228](https://github.com/raroche/NetPdf/pull/228), `a8be602`) + a 5-finding review. PR 4 (compositing + navigation — 5 tasks: border-image 9-slice, mask/mask-image on `<img>`, mix-blend-mode `/BM`, hyperlink Link annotations, document outlines) is DONE on branch `phase4-compositing-navigation` (gates + AOT `2942DD1E…` green; PR pending).** **PR 4 (compositing + navigation) MERGED ([#229](https://github.com/raroche/NetPdf/pull/229), `bcfe6ba`) + a 5-finding review. PR 5 (image pipeline) was found ALREADY COMPLETE (134 tests) — the next work landed SVG part 1 + the Skia subtree raster target (`SubtreeRasterizer`), MERGED ([#230](https://github.com/raroche/NetPdf/pull/230), `44321e2`).** **PR 7 (SVG part 2) is DONE on branch `phase4-svg-part2`** (off `44321e2`): gradient paint servers (`SvgPaintServers` — linear/radial `url(#id)`, objectBoundingBox + userSpaceOnUse, spreadMethod, gradientTransform, stops, radial focal, href inheritance), `<text>`/`<tspan>` (`SvgText` — Skia shaping, `text-anchor`, dx/dy, font props, gradient fill), `<use>`/`<symbol>`/`<defs>` (clone with inherited paint), + `fill-opacity`/`stroke-opacity` + `skewX`/`skewY` (gates + AOT `2942DD1E…` green; PR pending). **NEXT SESSION = the IPaintTarget seam** (drive `FragmentPainter`/`TextPainter` subtree painting onto an `SKCanvas` → feed `SubtreeRasterizer` → closes the general-element filter / mask / isolated mix-blend-mode deferrals — the raster bridge is no longer the blocker) **OR PR 8 (visual-regression harness — measures Phase-4 exit criteria 1-3)**; resume from this block + the Phase-4 roadmap.
>
> **Current state (2026-06-22):** Phase 3's layout + pagination engine drives multi-page rendering for tables, flex, grid, multicol, prose, empty/explicit-height blocks. Conformance is MEASURED (curated suite, PR 1 [#202](https://github.com/raroche/NetPdf/pull/202)). **ALL ELEVEN Phase-3 exit criteria are MET** — CSS 2.2 **100% (30/30)**, Fragmentation **100% (12/12)**, Flexbox 100% (19/19), Grid 100% (15/15); perf + memory gated. (Conformance was raised by the post-`0.7.0-beta` sizing-residuals + CSS Fragmentation control PRs, then to CSS 2.2 100% by the Phase-3 closeout PR.) The post-conformance **Flex L1 completion** PR then shipped real-world flex correctness (no conformance bump — Flexbox is already 100%): `align-items`/`align-self: baseline` (row cross-axis), intrinsic `flex-basis` (`content`/`max-content`/`min-content`, nowrap row main axis), + a `flex:1` layout-level integration test. The CSS 2.2 box-model gaps closed first ([#203](https://github.com/raroche/NetPdf/pull/203): box-sizing block-axis + emit/measure consistency + floats; min/max clamping explicit + auto/fill). Then the **flex/grid gap PR** closed: `gap`/`column-gap`/`row-gap` gutters (flex + grid) + flex/grid containers honoring their own explicit `width` — taking Flexbox 83.3% → 100% (criterion 4 MET) + Grid 80% → 93.3%. The **Phase-3 closeout PR** (post-beta) then closed all three remaining residuals (see below). **PR 8 — the `0.7.0-beta` release — is MERGED** ([#205](https://github.com/raroche/NetPdf/pull/205), squash `af1c210`): deferral audit + CHANGELOG `0.7.0-beta` + exit-criteria sign-off + the version bump (Directory.Build.props + version.json → 0.7.0-beta, guarded by `ReleaseVersionParityTests`); the **`0.7.0-beta` git tag is APPLIED** (annotated, → `af1c210`, pushed to origin 2026-06-21 — internal milestone tag on the private repo; NuGet ships at v1.0). The **post-`0.7.0-beta` sizing-residuals PR is MERGED** ([#206](https://github.com/raroche/NetPdf/pull/206), squash `825449b`): closed three sizing deferrals — grid `fr` tracks subtract gutters (Grid → 100%), percentage min/max-width/height (CSS 2.2 → 96.7%), percentage `column-gap`/`row-gap` (new flex case) — plus a review round (Roland [P1]/[P2] + Copilot) hardening the gutter accounting end-to-end: grid auto-fill/auto-fit count is gutter-aware, §11.6 Maximize Tracks subtracts gutters for non-fr tracks, flex `%` gaps + `%` item min/max resolve in the pre-measure (not just emission), and multicol `%` column-gap resolves against the content inline size. The **Phase-3 closeout PR** (post-beta) closed the last three residual deferrals: **`auto-height-emit-vs-pagination`** (auto-height block backgrounds span their children, capped to the page → **CSS 2.2 100%, 30/30**), **`inline-only-block-line-splitting`** (a single paragraph taller than a page splits its own lines across pages with orphans/widows — narrowed to chrome'd/atomic blocks), and **`multi-page-allocation-churn`** (a fragmenting table is measured ONCE per conversion + reused across pages via a cross-page `TableMeasurementCache` → per-page allocation O(n²) → ~O(1), **exit criterion 8 now fully met**, allocation-slope gated). Perf criterion 7 stays smoke-gated (full image + web-font benchmark is a Phase-4 corpus task).
>
> Last merged: PR [#207](https://github.com/raroche/NetPdf/pull/207) (**CSS Fragmentation control**, squash `0678940`) — `break-before`/`break-after` (+ legacy `page-break-*` aliases) propagate forced breaks through fitting ancestors AND to every in-flow block-level child (block-flow, inline-only/prose, nested table/flex/grid) via a single recursion-loop-top calculation, taking **Fragmentation 9/10 → 12/12 (100%)**; `break-*:avoid` set the boundary AvoidBreak flag (optimizer-honored); `orphans`/`widows` registered + read off the document **body** box into `BreakResolver`. Survived two review rounds (Roland [P1]/[P2] + Copilot). Before it: PR [#206](https://github.com/raroche/NetPdf/pull/206) (sizing residuals, squash `825449b`); the **`0.7.0-beta` git tag is APPLIED** (→ `af1c210`).
>
> Last merged: PR [#208](https://github.com/raroche/NetPdf/pull/208) (**Flex L1 completion**, squash `56796c0`) — real-world flex correctness, no conformance bump (Flexbox already 100%): (1) `align-items`/`align-self: baseline` first-baseline alignment on the row cross axis (was decoding to `stretch`; COLUMN + wrap-reverse fall back to flex-start); (2) intrinsic `flex-basis` (`content`/`max-content`/`min-content`) on the nowrap row main axis — measured via `FlexLayouter.BuildRowIntrinsicMainBaseSizes`, a shared helper the emission AND the BlockLayouter row-flex pre-measure both call so the §9.7 widths can't desync; the keyword AngleSharp.Css drops is recovered through `CssPreprocessor`; (3) a `flex:1` layout-level integration test + deferral/matrix/CHANGELOG cleanup. **Review round** (Roland P1/P2/P3 + Copilot, all 5 threads resolved): [P1] baseline-down-shifted items grow the line extent + the BlockLayouter pre-measure MIRROR (shared `ComputeItemBaselineAndCrossSize` / `ComputeBaselineAdjustedLineExtent`) so an auto-height wrapper contains them + a sibling doesn't overlap; [Copilot] text-less §8.5 baseline uses block chrome (not 0), column/wrap-reverse fallback mirrors flex-start's swap, baseline branch gated `!isWrapReverse`; [P2] min-content measures in intrinsic-sizing mode (`NestedContentMeasurer.intrinsicSizingMode`) so `overflow-wrap:break-word` isn't collapsed to glyph width; [P3] stale flex-basis docs refreshed. LTR/in-flow byte-identical (snapshot/golden/real-doc/conformance/AOT all green). Before it: PR [#207](https://github.com/raroche/NetPdf/pull/207) (Fragmentation control, `0678940`) + [#206](https://github.com/raroche/NetPdf/pull/206) (sizing residuals, `825449b`); the **`0.7.0-beta` git tag is APPLIED** (→ `af1c210`).
>
> **Phase 4 (Visual Parity) IN PROGRESS** — Phase 3's exit criteria are MET + `0.7.0-beta` is tagged, so the phase is unblocked ([docs/phases/phase-4-visual-parity.md](docs/phases/phase-4-visual-parity.md)). **PR 1 — native PDF gradients — is MERGED** ([#209](https://github.com/raroche/NetPdf/pull/209), squash `b902f4c`): `linear-gradient` → PDF axial shading (ShadingType 2) + `radial-gradient` → radial shading (ShadingType 3), over a greenfield shading foundation (`RegisterAxialShading`/`RegisterRadialShading` + `BuildGradientFunction` Type 2 / Type 3-stitch + `PdfPage.PaintShadingInRect` clip-and-`sh`). Survived a full review round (Roland's 5 findings + 8 Copilot threads, all resolved): strictly-increasing FunctionType 3 `/Bounds` for terminal hard stops; aspect-ratio-correct `to <corner>`; radial `at <position>` axis classification (rejects duplicate/misordered); function + shading reuse cache; multi-layer-list rejection; doc/registry sync. **PR 2 — shadows + 2D transforms — is MERGED** ([#210](https://github.com/raroche/NetPdf/pull/210), squash `de7a8ff`): (1) ✅ **`box-shadow`** outset — sharp → native filled (rounded) rect (offset + spread); blurred → a new Skia raster bridge (`ShadowRasterizer` → image XObject + alpha `/SMask`, `CSS-BOXSHADOW-BLUR-RASTER-001`); inset + bad-unit deferred (`CSS-BOXSHADOW-UNSUPPORTED-001`). (2) ✅ **`text-shadow`** — the glyph run offset in the shadow color UNDER the text (sharp native; blur approximated sharp + `CSS-TEXTSHADOW-UNSUPPORTED-001`). (3) ✅ **2D `transform`** (`translate`/`scale`/`rotate`/`skew`/`matrix` + axis variants) → a PDF `cm` about `transform-origin` wrapping BOTH the decoration (FragmentPainter) and the glyphs (TextPainter); 3D flattens (`CSS-TRANSFORM-3D-UNSUPPORTED-001`); `transform`/`transform-origin` recovered via `CssPreprocessor`. All gates green; non-shadow/non-transform rendering byte-identical. After #210, the **Phase-3 closeout PR** (this branch) interrupted Phase 4 to shut the last three Phase-3 residual deferrals (auto-height emit → CSS 2.2 100%, single-paragraph line splitting + orphans/widows, table allocation O(n²) → O(1)/page). **Next:** resume Phase 4 — refinements (gradient ellipse-via-CTM, `repeating-*`, conic-raster, per-stop alpha; box-shadow inset; text-shadow inheritance + glyph blur; transform faithful-3D-projection + stacking context — all in `deferrals.md`), or the next Phase-4 feature group (filters / SVG). `git log --oneline -1` shows the exact commit.
>
> This file was consolidated from a 1.1 MB chronological log on 2026-06-18; the full per-subtask history is archived in [docs/progress-archive.md](docs/progress-archive.md). **Keep this file compact** — roll the roadmap as each PR lands; don't grow a blow-by-blow log here.

## Status at a glance

| Phase | Scope | State |
|---|---|---|
| 0 | Legal & architecture lock | ✅ Complete |
| 1 | PDF writer + text foundation | ✅ Complete |
| 2 | CSS engine + DOM pipeline | ✅ Complete |
| 3 | Fragmentainer-aware layout + pagination | ✅ **Complete — ALL exit criteria MET (CSS 2.2 100% / Frag + Flex + Grid 100%; perf + memory gated); `0.7.0-beta` TAGGED (`af1c210`); closeout PR shut the last 3 residuals** |
| 4 | Visual parity (gradients, shadows, filters, SVG) | 🚧 **ACTIVE PHASE** — transforms (#210) + linear/radial gradients (#209) + sharp/blurred shadows (#210) + backgrounds (Phase-3 interleaved) MERGED (~15-20%); filters / clip-path / mask / SVG / links / outlines / visual-regression harness remain. See the Phase-4 roadmap below. |
| 5 | Packaging + release | 🔵 Interleaved — layout→PDF wiring done |

## Phase 4 — remaining-work roadmap (active phase, 2026-06-25)

Worked as **3-task PR groups** (the project cadence), ordered by value + dependency. The full per-task breakdown + spec links + the native-vs-raster decision rule live in [docs/phases/phase-4-visual-parity.md](docs/phases/phase-4-visual-parity.md) (its work-breakdown table is annotated ✅ for the merged tasks). The **Skia raster-fallback bridge is established** (`ShadowRasterizer` → image XObject + alpha `/SMask`, with a once-per-conversion diagnostic) — conic gradients, filters, blurred effects, complex clip-path, and masks all reuse that pattern.

**DONE (merged):** 2D transforms (#210), linear + radial gradients (#209), sharp + blurred box-/text-shadow (#210), backgrounds + `object-fit`/`-position` (Phase-3 interleaved), `border-radius` Bézier corners (Phase-3).

| PR | Group | Tasks (phase-4 doc #) |
|---|---|---|
| **1** ✅ **MERGED-PENDING** | Gradient / shadow **refinements** (finish #209/#210 residuals) | ✅ conic-gradient / repeating-conic → Skia sweep raster (`CSS-CONIC-GRADIENT-RASTER-001`); ✅ length-positioned color stops (px + absolute units); ✅ `repeating-linear`/`-radial` → native stop tiling; ✅ radial **ellipse-via-CTM**; ✅ box-shadow **inset** (native ring + blurred raster). Branch `phase4-gradient-shadow-refinements`. Residuals → PR-later: per-stop alpha (native soft-mask), color hints, multi-layer lists, text-shadow blur, transform 3D/stacking |
| **2** ✅ **MERGED-PENDING** | **CSS filters on IMAGES** (`phase4-css-filters`) | ✅ `CssFilter_Parser` (10 functions) + ✅ Skia `ImageFilterApplier` color matrices (grayscale/sepia/invert/brightness/contrast/saturate/hue-rotate/opacity) + ✅ `blur()` + ✅ `drop-shadow()` (alpha-following, bounds-expanding) + ✅ chaining + the **non-replaced-element deferral** (`CSS-FILTER-ELEMENT-UNSUPPORTED-001`). `CSS-FILTER-RASTER-FALLBACK-001`. **DEFERRED:** general element-subtree filters (needs a Skia subtree renderer — Roland chose "image filters first"), `filter:opacity()`→native ExtGState + native `opacity`, filters on background-images |
| **3** ✅ **MERGED** | **Borders + clip-path** ([#228](https://github.com/raroche/NetPdf/pull/228), `a8be602`) | ✅ dashed/dotted (PDF stroke+dash); ✅ double + 3D groove/ridge/inset/outset; ✅ `clip-path` native inset/circle/ellipse/polygon (decoration + `<img>`); + a 5-finding review (stroke `/CA`; `polygon(evenodd)`→`W* n`; `CSS-CLIP-PATH-UNSUPPORTED-001`; dotted `[0 2w]` dots; circle/ellipse side keywords). **DEFERRED:** rounded non-solid borders (solid-ring approx); `clip-path: path()` raster; clip-path subtree (Skia subtree renderer) |
| **4** ✅ **DONE (branch `phase4-compositing-navigation`)** | **Compositing + navigation** (5 tasks) | ✅ **border-image** 9-slice (stretch; `PlaceImageSlice`); ✅ **mask**/`mask-image` on `<img>` (Skia alpha → `/SMask`, `CSS-MASK-RASTER-FALLBACK-001`; general elements diagnosed); ✅ **mix-blend-mode** → native `/BM` ExtGState; ✅ hyperlink **Link annotations** (`<a href>`→`/URI`, behind the preflight's narrow opt-in); ✅ document **outlines** (`<h1>`–`<h6>`→`/Outlines`). **DEFERRED:** border-image tiling/width/outset; general-element mask + luminance; blend text/image + isolation; inline-flow + `#fragment` links — see deferrals.md |
| **5** ✅ **ALREADY DONE (earlier cycles)** | **Image pipeline refinements** | ✅ JPEG passthrough + PNG/Flate + `SMask` (19, 20); ✅ WebP/AVIF via Skia + GIF first-frame (21, 22 — **AVIF blocked on macOS** SkiaSharp 3.119); ✅ CMYK incl. Adobe-inverted `/Decode` (23). Verified complete (134 image tests) — no new work; the next PR did SVG instead. |
| **6** 🟡 **PART 1 DONE (branch `phase4-svg-part1`)** | **SVG part 1** + the **Skia subtree raster target** | ✅ `SubtreeRasterizer` (SKCanvas→RGBA XObject — the deferred subtree-renderer foundation); ✅ `SvgRasterizer` shapes/paths/transforms/fills/strokes (rect/circle/ellipse/line/poly*/path/`<g>`) → raster, wired through `<img>` SVG sources; ✅ `image/svg+xml` re-enabled (XXE-safe renderer). **DEFERRED to PR 7:** `<text>`, gradients, `<use>`/`<symbol>`/`<defs>`, `<image>`, native vector SVG→PDF, inline `<svg>` layout. |
| **7** ✅ **DONE (branch `phase4-svg-part2`)** | **SVG part 2** | ✅ SVG gradient paint servers (`SvgPaintServers` — linear/radial `url(#id)`; objectBoundingBox + userSpaceOnUse, spreadMethod, gradientTransform, stops, radial focal, href inheritance); ✅ `<text>`/`<tspan>` (`SvgText` — Skia shaping; `text-anchor`, dx/dy, font props, gradient fill); ✅ `<use>`/`<symbol>`/`<defs>` (clone with inherited paint) + `fill-opacity`/`stroke-opacity` + `skewX`/`skewY`. **DEFERRED:** `<image>`, `<pattern>`, `%`/`em` lengths, `stroke-dasharray`, element clip/mask/filter refs, `textPath`/complex-script shaping, native vector SVG→PDF, inline `<svg>` layout. |
| **8** | **Visual-regression harness + release** (measures exit criteria 1-2, 9-10) | pinned-Chrome Docker + reference-PNG generator (30, 31); pixel-diff runner SSIM + per-pixel + CI (32, 33); generate references + close visual deltas + tag `0.9.0-rc1` (34, 35, 36) |

**Sequencing note:** PRs 1-7 are feature work; **PR 8 (the visual-regression harness) is what MEASURES Phase-4 exit criteria 1-3** — it can be stood up earlier (its tasks 30-33 have no feature deps) to make every later PR measurable against Chrome, but the corpus-wide delta-closing (35) naturally comes last. Filters/clip-path/mask must engage the raster fallback with the **correct diagnostic codes** (exit criterion 3) — register each `CSS-…-UNSUPPORTED` / `…-RASTER` code as the feature lands.

**Gates (all green, 2026-06-26):** 7730 unit / 3 skip (+ the 4 perf/memory gates incl. the allocation-slope gate) · 30 LayoutSnapshots · 97 RealDocuments · **W3cConformance (4 per-case-baseline gates; published rates CSS 2.2 100% / Frag 100% / Flex 100% / Grid 100%)** · PaginationGolden · RenderingCorpus · PdfValidation · 0-warning Release · AOT/JIT parity (`2942DD1E…`) · determinism.

## Phase 3 — what's shipped (consolidated)

- **Pagination engine (`NetPdf.Paginate`)** — break resolver, documented cost model, bounded-DP optimizer (≤2-page lookahead), continuation tokens, checkpoint/rewind, bounded-retry coordinator.
- **Layouters (`NetPdf.Layout`)** — Block (margin-collapse, BFC, min/max/fit-content, floats), Inline + LineBuilder (UAX#9 bidi, HarfBuzz shaping, UAX#14 breaking, wrap, white-space, `text-align` incl. justify/justify-all, full `vertical-align`), FloatManager + `clear`, Table (auto/fixed, collapse/separate, row/colspan, `<thead>`/`<tfoot>` repeat), Multicol, Flex L1 (single + multi-line + column split + item content), Grid L1 (track sizing, placement, dense, areas, implicit rows), Absolute + `position: fixed`.
- **Multi-page driver** (cycles 0–8, PRs [#175](https://github.com/raroche/NetPdf/pull/175)–[#179](https://github.com/raroche/NetPdf/pull/179)) — nested-container fragmentation, the page-emitting driver loop, per-page counters, cross-page running content, per-page `@page :first/:left/:right/:blank`, named pages, font-dedup across pages.
- **Paged media + generated content** — `@page` rules + the 16 margin boxes (style/border/padding/background/border-radius), `string()`/`string-set` (incl. `content()`), `element()`/`position: running()` (own font/colour/decoration), `counter(page)`/`counter(pages)` with counter styles.
- **Paint (Phase-5-interleaved)** — TextPainter (shaping → subset → embed), FragmentPainter (background-color/-image, borders, outline, border-radius, tiling patterns), image pipeline (`<img>` + `background-*`, `object-fit`/`-position`, data: URIs, Skia raster fallback).
- **Cross-cutting** — determinism gated, AOT/JIT parity gated, 0-warning Release, banned-dependency analyzer.

## Phase 3 — exit-criteria status

Phase 3 is "complete" per [phase-3 §Exit criteria](docs/phases/phase-3-layout-and-pagination.md) when all 11 hold:

| # | Criterion | Status |
|---|---|---|
| 1 | 4 invoice corpus files render to a valid PDF | ✅ |
| 2 | Anvil sample: footer + "Page N of M" on every page | ✅ (multi-page + counters live) |
| 3 | W3C CSS 2.2 layout pass-rate ≥ 90% | ✅ **MEASURED 100%** (30/30) — MET (box-sizing + min/max incl. percentage, emit/measure consistent; auto-height emit spans children, capped to the page — `auto-height-emit-vs-pagination` closed) |
| 4 | W3C Flexbox pass-rate ≥ 85% | ✅ **MEASURED 100%** (19/19) — MET (gap gutters incl. percentage + container honors own `width`) |
| 5 | W3C Grid L1 pass-rate ≥ 70% | ✅ **MEASURED 100%** (15/15) — MET (gap gutters + `fr` tracks subtract the gutters from their free space) |
| 6 | W3C Fragmentation pass-rate ≥ 80% | ✅ **MEASURED 100%** (12/12) — MET (`break-before`/`break-after` + legacy `page-break-*` forced breaks propagate through fitting ancestors) |
| 7 | Perf: 3-pg ≤ 200 ms, 20-pg ≤ 1.5 s p50 | 🟡 **signed off measured-with-documented-residuals** — layout-pipeline smoke-gated (`PerformanceGateTests`: 3-pg ~42 ms, 22-pg ~400 ms — synthetic fonts + table content). The FULL-pipeline target (tables + **images + web fonts**, docs/design/performance.md) is the BenchmarkDotNet flow, not yet a build gate. |
| 8 | Memory linear with page count | ✅ **MET** — RETAINED heap flat (gated) AND per-page ALLOCATION ~constant across page count (gated, allocation-slope test): a fragmenting table is measured ONCE/conversion + reused across pages via the cross-page `TableMeasurementCache` → ~O(n²) → ~O(1)/page (`multi-page-allocation-churn` closed). |
| 9 | AOT smoke passes | ✅ |
| 10 | Determinism | ✅ |
| 11 | CHANGELOG + `0.7.0-beta` tagged | ✅ **DONE** — CHANGELOG `0.7.0-beta` entry written + exit-criteria sign-off (PR 8); the annotated `0.7.0-beta` git tag is APPLIED (→ `af1c210`, pushed 2026-06-21). |

**Bottom line:** **PHASE 3 IS COMPLETE — ALL 11 exit criteria MET.** Conformance: CSS 2.2 **100%** / Fragmentation 100% / Flexbox 100% / Grid 100%. Perf + memory (criteria 7–8) gated (criterion 8 now fully met — retained-heap AND allocation-slope). PR 8 (`0.7.0-beta` release) is MERGED ([#205](https://github.com/raroche/NetPdf/pull/205), `af1c210`) and **TAGGED**. The post-beta **sizing-residuals PR** closed three sizing deferrals; the **Phase-3 closeout PR** then closed the last three residual deferrals — `auto-height-emit-vs-pagination` (CSS 2.2 → 100%), `inline-only-block-line-splitting` (narrowed: text-only line splitting + orphans/widows ships), `multi-page-allocation-churn` (criterion 8 met). No Phase-3 residual blocks anything.

## Phase 3 — remaining-work roadmap

Worked as **3-task PRs** (complete 3 → review → merge → next 3), in order. PRs 1–7 + the CSS 2.2 box-model PR + the flex/grid gap PR are DONE. **All four conformance exit criteria are MET.** PR 8 (the **`0.7.0-beta` release**) is MERGED ([#205](https://github.com/raroche/NetPdf/pull/205)) and TAGGED (`0.7.0-beta` → `af1c210`). A post-beta **sizing-residuals PR** ([#206](https://github.com/raroche/NetPdf/pull/206)) then closed three sizing deferrals (grid `fr`+gap, percentage min/max, percentage gaps), taking Grid + Flexbox to 100% and CSS 2.2 to 96.7%.

### CSS 2.2 box-model gaps  [clears criterion 3] ✅ DONE
1. ✅ **`box-sizing: border-box` (block axis)** — the recursive subtree emitter added padding/border OUTSIDE the declared height (inline axis already honored box-sizing); routed it through `BoxSizingHelper`. Flips `css22-box-sizing-border-box` + 3 new cases.
2. ✅ **min/max-width/height clamp a size** (§10.4/§10.7) — added `ClampBorderBoxToMinMax` (block mirror of the flex min/max reader), applied at the in-flow width/height sites (explicit + auto/fill) + the subtree measure + floats (review fixes — emit/measure consistency). Flips `css22-min-width-on-explicit` + 7 new cases. **CSS 2.2 → 93.3% (28/30), criterion 3 MET.** (percentage min/max → `min-max-percentage-sizing` deferral.)
3. ✅ **auto-height shrink-to-fit** — first attempt (this PR) emitted the effective content-spanning height unconditionally → regressed multi-page block-flow pagination (forced-overflow), so it was reverted + deferred (`auto-height-emit-vs-pagination`); criterion 3 was MET without it. **Later CLOSED by the Phase-3 closeout PR** — the emitted auto-height fragment now spans its children CAPPED to the page fragment (the cap is what avoids the force-overflow), flipping `css22-auto-height-contains-child` → **CSS 2.2 100% (30/30)**.

### PR 1 — Conformance measurement  [criteria 3–6] ✅ DONE (PR [#202])
1. ✅ **Harness** — replaced the `NetPdf.W3cConformance` smoke stub with a CURATED assertion suite (Roland's A/B call: curated NetPdf cases over vendored WPT). Drives the internal pipeline (`Phase2Pipeline`→`BlockLayouter`) + asserts `BoxFragment` geometry. CSS 2.2 subset (19 cases) → **84.2%**.
2. ✅ **CSS 2.2 + Fragmentation pass-rates** — gated at regression floors met today; Fragmentation (10 cases) → **90.0%** (MET ≥80%).
3. ✅ **Flexbox + Grid pass-rates** — Flexbox (12) → **83.3%** (below ≥85%, container-width gap counted head-on per review); Grid (10) → **80.0%** (MET ≥70%); four numbers published in `tests/NetPdf.W3cConformance/README.md`. Gates assert a **per-case baseline** (every non-`KnownGap` case must pass; every `KnownGap` case must still fail) — not a pass-rate floor (PR 1 review [P1]); the published rates sit next to their roadmap targets.

### PR 2 — Direction / bidi pipeline  [feature; several deferrals block on this]
4. ✅ A shared `direction` resolution pipeline — `DirectionStyleExtensions` (`ReadDirection`/`IsRtl`/`ReadParagraphDirection`); `direction` registered (inherited, `ltr`/`rtl`); bidi base direction now CSS-driven at the inline-layout seam. Writing-mode stays horizontal-tb (the seam composes vertical modes later).
5. ✅ RTL `text-align` start/end swap (`ReadInlineAlignFactor` direction-aware — `start`→right in RTL) + RTL inline-atomic alignment (atomic shifts to the right edge). LTR output byte-identical.
6. ✅ RTL flex main-axis flip (`flex-direction: row` under `direction: rtl`) — FlexLayouter XORs its reverse flag via `IsRtl` (row+rtl ≡ row-reverse+ltr); LTR byte-identical. *Residual direction gaps* (see `rtl-fragment-reversal`): UAX #9 L2 slice reversal, `dir` HTML attribute → `direction`, margin-box base direction — all CLOSED in the residual long-tail batch; flex COLUMN cross-axis RTL per-item anchor SHIPPED there too (only the MULTI-LINE wrap line-stacking remains).

### PR 3 — Inline-text polish  [feature]
7. ✅ Line-edge `vertical-align` line growth — a tall `top/bottom/middle/text-*` run now GROWS its line (`InlineVerticalAlign.TextLineEdgeGrowth`); the painter follows via the shared per-line metrics.
8. ✅ `line-height` cascade wiring — `LineHeightResolver` + `ReadLineHeightPx` resolve the full `normal | <number> | <length> | <percentage>` grammar (was UNWIRED → silently font-size × 1.2). (`%` inherit-as-length — `line-height-percentage-inheritance` — CLOSED in the residual long-tail PR: % resolves to a length at the declaring element's font-size before it inherits.)
9. ✅ inline-block per-run baseline metrics (`BufferingMeasureSink.DeepestLastLineRunStyle` — deepest last-line run drives the descent) + justify-all on internal `<br>` lines (lifts the §7.3 forced-break exception under justify-all).

### PR 4 — Paged-media completion  [feature]
10. ✅ Running-element nested **block** layout — already rendered via the segment-style + container-bands cycles (stacked lines per block child + wrapping + per-block own-style + decorated container bands); confirmed + deferral corrected. Residual: inline-level styling WITHIN a leaf block.
11. ✅ `string(name, start)` / `first-except` (the page entry value; first-except empty when first == start) + compound `@page` selectors (`chapter:first` etc. — already live in the multi-page path; stale roadmap item). element() start/first-except stays deferred.
12. ✅ Page-margin box overflow + container-relative units — CONFIRMED done: overflow (vertical line-granularity truncation + horizontal glyph clip-path + `overflow:visible` opt-out + `PAINT-MARGIN-BOX-CONTENT-OVERFLOW-001`) and `%`/`em`/`vw`/`calc()` resolution are implemented + tested; container units (`cqw`/…) are a tracked **post-v1 (v1.4)** deferral, already diagnosed + dropped + tested. (`margin-box-line-height` — CLOSED in the residual long-tail PR: a declared/inherited `line-height` incl. `%` / `em` / `rem` / `calc()` drives the margin-box pitch.) Residual: border-radius `calc()` (minor), flex COLUMN cross-axis RTL MULTI-LINE line-stacking (the per-item anchor shipped in the residual long-tail batch).

### PR 5 — Perf + memory gates  [criteria 7–8] ✅ DONE
13. ✅ 3-page invoice ≤ 200 ms p50 — enforced `PerformanceGateTests` (~42 ms, synthetic-font table invoice).
14. ✅ 20-page report ≤ 1.5 s p50 — enforced gate (~400 ms, 22-page tabular report; the live multi-page path).
15. ✅ Retained-heap gate — heap flat across page count. (Criterion 8 was PARTIAL at the beta; the Phase-3 closeout PR then added the per-page allocation-slope gate + the cross-page `TableMeasurementCache` fix, so allocation scaling is now ~O(1)/page — criterion 8 fully MET.)

### PR 6 — PROSE PAGINATION  ✅ DONE
16. ✅ **Inline-only block pagination** (block-granularity) — the recursion's inline-only branch now consults the break resolver, so a text block whose margin-box overflows the page breaks WHOLE to the next page (`<p>×200` paginates; no content loss/duplication). Guards a real content extent (`InlineOnlyBreakMinExtentPx`) so a ZERO-extent anonymous block (flex/grid content past the page edge) doesn't spuriously break — the regression that blocked the first cut, root-caused (both triggers were `chunk == 0` at `start > pageBlockSize`). Residual at the time: line-level paragraph splitting / orphans+widows (`inline-only-block-line-splitting`) — **since SHIPPED by the Phase-3 closeout PR** (a single text paragraph taller than a page slices its own lines with orphans/widows; NARROWED to the chrome'd/atomic fallback). Full unit suite + snapshots/golden/real-docs all green.

### PR 7 — Pagination / table / grid hardening  [feature] ✅ DONE
17. ✅ **Table intra-cell row splitting** (block-granularity) — a body row whose cell stacks block children taller than the page breaks WITHIN itself across pages (`TableContinuation.RowSplitOffset`; cells measure full natural height via `SuppressBlockPagination`; split-aware dry-run propagates the continuation through the production `BlockLayouter` path). Tight scope: no footers/bottom-captions/rowspan-origin; a single atomic block still force-overflows (`inline-only-block-line-splitting`).
18. ✅ **Grid cross-page row-extent memo** + corrected the deferral premise — the 3 `GridSizing.Resolve` sites take GENUINELY different inputs (indefinite-block site 1 vs definite sites 2/3; probe lacks measurers), so a naive 3→1 share is INCORRECT for `fr` grids; the expensive shaping was already shared. Landed the page-invariant site-1 §11 memo (`GridMeasurementCache.RowExtentSum`) so resume pages skip it; site-2+3 collapse needs a reftest sweep (stays deferred).
19. ✅ **Float content atomicity** — nested grid + flex in a float emit atomically (lossless) instead of paginating-then-truncating (floats don't fragment yet; `_inAtomicFloatSubtree`). Residual (all `not-started`/`approximated`, documented): nested table+multicol in floats still truncate (page-budget-coupled wrapper sizing); recursive consumed-extent accounting is LATENT (no visible impact). (`multi-page-allocation-churn` was closed by the Phase-3 closeout PR.)

### PR 8 — Release  [criterion 11] ✅ MERGED ([#205](https://github.com/raroche/NetPdf/pull/205))
20. ✅ **Deferral audit** — reconciled `deferrals.md` / `compatibility-matrix.md` with live state: #203/#204 had already struck flex/grid container-width + documented box-sizing (BLOCK/GRID/TABLE/FLEX) + LengthPx min/max as shipped; refreshed the stale `flex-layouter-features` Status (Flexbox is 100%, gap shipped) + the matrix footer.
21. ✅ **CHANGELOG `0.7.0-beta` + exit-criteria sign-off** — added the `[0.7.0-beta]` CHANGELOG entry (staged-for-tagging pattern); signed off the phase-3 exit criteria + PROGRESS criterion 11: conformance 3–6 MET, perf/memory 7–8 measured-with-documented-residuals (smoke-gated). The review round also bumped the actual version (Directory.Build.props + version.json → 0.7.0-beta, guarded by `ReleaseVersionParityTests`) + rewrote the README to the facade-wired state.
22. ✅ **Tag `0.7.0-beta`** — APPLIED (annotated tag → `af1c210`, #205's merge commit, pushed to origin 2026-06-21) so the tag captures the signed-off state before post-beta work. Repo is PRIVATE → an INTERNAL milestone tag, NOT a public release, NO NuGet (that's v1.0).

### Post-`0.7.0-beta` — sizing residuals  ✅ MERGED ([#206](https://github.com/raroche/NetPdf/pull/206), squash `825449b`)
Three left-behind sizing deferrals, each raising a conformance category (all gates + AOT green; LTR/in-flow byte-identical):
1. ✅ **grid `fr`+gap** (`grid-gap-fr-track-sizing`) — `ResolveFrTracks` subtracts the `(n-1)*gap` gutter total from the fr leftover (percentage tracks still resolve against the full extent). Flips `grid-fr-columns-with-gap` → passing. **Grid 14/15 → 15/15 (100%).**
2. ✅ **percentage min/max** (`min-max-percentage-sizing`) — `ClampBorderBoxToMinMax` takes a containing size; a `%` min/max resolves against it (indefinite → none/0 per §10.7), threaded through all 9 in-flow clamp sites using each site's own `%`-width/height base. Flips `css22-min-width-percentage` → passing. **CSS 2.2 28/30 → 29/30 (96.7%).**
3. ✅ **percentage gaps** (`gap-percentage-sizing`) — `column-gap`/`row-gap` → `LengthPercentage`; `ReadFlexGridGapOrZero` resolves a `%` against the matching content extent (column-gap → inline, row-gap → block), threaded at the flex + grid emission sites. Adds `flex-percentage-column-gap` (Flexbox → 19/19, still 100%).
4. ✅ **review round** (Roland [P1]/[P2] + Copilot) — hardened the gutter/percentage accounting end-to-end so pre-measure and emission can't desync: grid auto-fill/auto-fit count is gutter-aware (§7.2.3.1); §11.6 Maximize Tracks subtracts gutters for non-fr growable tracks; flex `%` gaps resolve in the BlockLayouter pre-measure (a `%` row-gap on an auto-height column resolves to 0 per the indefinite-reference rule); flex item `%` min/max main-size resolves against the container main size; multicol `%` column-gap resolves against the content inline size (was silently `normal`). +13 unit tests.

### CSS Fragmentation control  ✅ MERGED ([#207](https://github.com/raroche/NetPdf/pull/207), squash `0678940`) [Fragmentation criterion 6 → 100%]
The `BreakResolver` engine already had forced-break (`ForceBreak` → `BreakHere`) + orphans/widows + `break-inside: avoid` cost; this PR wired the CSS:
1. ✅ **`break-before` / `break-after`** (+ legacy `page-break-before`/`-after` aliases) — registered in `properties.json` + KeywordResolver; `ForcesPageBreakBefore`/`After` readers (modern wins over legacy). Forced breaks propagate to BOTH break-decision sites: the top-level loop AND `EmitBlockSubtreeRecursive`'s nested-block decision (so a `break-before:page` on a grandchild splits its *fitting* `body`/`html` ancestors — the named-page forced-break precedent at ~L4951). **Frag 9/10 → 12/12 (100%)** (flips `frag-break-before-page` + adds `frag-break-after-page` + `frag-page-break-before-always`).
2. ✅ **`break-inside: avoid`** (+ `break-before:avoid` / `break-after:avoid`) — `AvoidsBreakInside` / `AvoidsPageBreak*` readers set the boundary `AvoidBreak` flag (every internal boundary of a `break-inside:avoid` box). Honored by the optimizing resolver's cost; the production greedy resolver is cost-insensitive (residual — block-flow children are already atomic so it's not visibly wrong today).
3. ✅ **`orphans` / `widows`** — registered (`Integer`, inherited, default 2) + `ReadOrphansOrDefault`/`ReadWidowsOrDefault`; `PdfRenderPipeline` reads the **body box** (NOT the synthetic root, which carries the initial default — PR #207 review [P2]) into `new BreakResolver(orphans, widows)`. The visible effect (then awaiting line-level splitting) **shipped in the Phase-3 closeout PR**: a split paragraph honors orphans/widows at the cut, read off the paragraph's OWN computed value (so per-paragraph overrides work — for split blocks).

Residuals (documented in `deferrals.md#fragmentation-control-residuals`): left/right/recto/verso parity blank-page insertion (left/right behave like `page` today; `recto`/`verso`/`all` aren't parsed by AngleSharp.Css 1.0.0-beta.144 yet); avoid-is-optimizer-only; per-paragraph orphans/widows.

### Flex L1 completion  ✅ SHIPPED (branch `phase3-flex-l1-completion`, 3 commits; `flex-layouter-features` deferral)
Real-world flex correctness, no conformance bump (Flexbox already 100%). LTR/in-flow byte-identical (all gates green). The `flex` shorthand was found ALREADY DONE on verification (`FlexShorthandExpander`, §7.4) — the slot it freed went to the integration test:
1. ✅ **`align-items: baseline` / `align-self: baseline`** — real first-baseline alignment on a ROW container's cross axis (was decoding to `stretch`). `AlignItemsValue.Baseline` decode; `BufferingMeasureSink.FirstBaselineFromOrigin` exposes the topmost line's baseline; `FlexLayouter` shifts each baseline-aligned item so its first text baseline sits on the line's max baseline (synthesized from the cross-end edge when an item has no line box, §8.5). COLUMN baseline falls back to flex-start.
2. ✅ **`flex-basis: content` / `max-content` / `min-content` (nowrap ROW main axis)** — `FlexLayouter.BuildRowIntrinsicMainBaseSizes` measures each item's max-content (no wrap pressure) / min-content (maximal wrap pressure) inline extent as the §9.7 flex base size; the SAME shared helper is called by the emission AND the BlockLayouter row-flex height pre-measure so the resolved widths can't desync. The keyword AngleSharp.Css drops is recovered through `CssPreprocessor`. COLUMN content/max/min-content content-sizes via the existing block-extent measure. WRAP rows + `fit-content` stay deferred.
3. ✅ **`flex:1` layout-level integration test + cleanup** — pinned `flex: 1` → grow 1 / shrink 1 / basis 0 end-to-end; struck the shipped items from `deferrals.md#flex-layouter-features` + the compatibility-matrix Flexbox row (also dropped its stale `%`-gutter dead-link known gap) + CHANGELOG `[Unreleased]`.

**Phase-3 closeout PR — DONE** (this branch, 3 commits): `auto-height-emit-vs-pagination` (CSS 2.2 → **100%**), `inline-only-block-line-splitting` (text-only line splitting + orphans/widows; narrowed), `multi-page-allocation-churn` (table cross-page measurement cache → criterion 8 met). **Phase 3 is now COMPLETE — all 11 exit criteria MET.** **Next:** resume **Phase 4** visual parity (gradient refinements / filters / SVG — see the Phase-4 paragraph at the top). Remaining flex residuals (WRAP intrinsic basis, `fit-content`, `align-content: baseline`, RTL column cross-axis MULTI-LINE line-stacking — the per-item anchor shipped) documented in `deferrals.md#flex-layouter-features`.

> **Backlog pool** (interleave into the PRs above as conformance findings dictate): flex `align-content: baseline`, flex `%`/`em`/`calc()` item sizing, multicol font-relative `column-width` + balancing cache, grid box-sizing/maximize/perf residuals (**audit first — many may be stale**), `outline` non-solid styles + diagnostic, `page`/`object-position` `@supports` registration, empty-resume-page sentinel cleanup, per-page abspos container geometry. Full inventory: the 2026-06-18 backlog sweep (34 items) is summarized in the conversation that produced this roadmap; ground each task against `deferrals.md` before starting.

## Where to look

- **Next action / live state:** the roadmap above; `git log --oneline -1`; or run `/phase-status`.
- **Deliberately deferred + pickup triggers:** [docs/deferrals.md](docs/deferrals.md).
- **In/out of scope:** [docs/compatibility-matrix.md](docs/compatibility-matrix.md).
- **Phase 3 plan + exit criteria:** [docs/phases/phase-3-layout-and-pagination.md](docs/phases/phase-3-layout-and-pagination.md).
- **Multi-page driver design + its remaining backlog:** [docs/design/multi-page-driver.md](docs/design/multi-page-driver.md).
- **Deep per-task history (pre-2026-06-18):** [docs/progress-archive.md](docs/progress-archive.md).
